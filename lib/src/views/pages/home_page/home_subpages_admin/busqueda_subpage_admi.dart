import 'package:flutter/material.dart';

class BusquedaSubPageAdmin extends StatelessWidget {
  const BusquedaSubPageAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BusquedaSubPageAdmin'),
      ),
    );
  }
}
